/Map
 ├── Map.h / Map.cpp          # lớp quản lý toàn bộ bản đồ (height, width, load/save, update)
 ├── Tile.h                   # định nghĩa TileType, Tile struct (loại địa hình, walkable, cost, sprite index)
 ├── TileMap.h / TileMap.cpp  # vẽ map bằng VertexArray + tileset (tối ưu draw)
 ├── MapLoader.h / MapLoader.cpp  # load/save map từ file tĩnh (txt/json) hoặc seed (random)
 ├── MapGenerator.h / MapGenerator.cpp  # tạo map ngẫu nhiên (Perlin noise / rule-based)
 ├── Quadtree.h / Quadtree.cpp # đã có, để spatial partition cho obstacles
 |- CameraController  
 └── Minimap.h / Minimap.cpp  # render minimap và UI liên quan
